# v1.2.1 TXT 白屏修复

修复旧浏览器缓存与新版阅读模块混用，导致 TXT 打开时页面白屏的问题。所有静态依赖现在使用整套内容版本目录，避免重复启动应用。

部署：将此压缩包上传到现有 Cloudflare Pages 项目 reader-8ux，或解压后上传本目录内容。部署根目录必须直接包含 _worker.js、_routes.json、index.html。保留现有数据库、对象存储绑定和环境变量。

部署成功后刷新网页，再打开 TXT。不需要删除书籍、清空浏览器数据或重建数据库。版本接口 /api/v1/version 应返回 cloud-reader-v1.2.1。

验证：本地模拟 v1.1 -> v1.2 白屏 -> v1.2.1 恢复，全程保留浏览器缓存与本地存储；长篇 TXT、整本下载、EPUB、多用户隔离以及静态依赖路径检查均通过。尚未替用户部署线上站点。
