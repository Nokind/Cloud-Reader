# Cloud Reader v1.2.0

## 更新内容

- 登录后直接进入书架，不再设置或输入独立的离线阅读密码。账号密码仍用于登录；本地缓存按用户 ID 分开，设备自动保存解密密钥。
- 阅读正文中移除了“完整下载 / 离线阅读”提示。阅读设置新增“打开时下载完整书籍”和“立即下载整本并阅读”。
- TXT 可选择按需分段加载或整本下载。EPUB、PDF 等现有解析器需要完整文件，关闭开关不会阻止这些格式的必要下载。
- 增加 12 种字体选项、10 套阅读配色、自定义正文和背景颜色、5 档字重、斜体和字间距。字体使用设备已安装字体，未安装时使用后备字体；设置随账号阅读记录同步。
- 新增 OneDrive、Google Drive、百度网盘、阿里云盘、WebDAV、Nextcloud 和 ownCloud 的云服务配置页面。

## 部署与旧数据

将本目录中的 `_worker.js`、`_routes.json`、`index.html` 上传到原有 Cloudflare Pages 项目。保留原 DB、BOOKS、DEPLOY_KEY 和 PASSWORD_PEPPER 配置。无需清空数据库，也不要执行工作目录中的旧版迁移 SQL。

新增云连接表和任务表会在首次访问云服务接口时自动创建。原用户、书籍及已同步的阅读记录继续使用。

旧版离线口令保护的数据无法在缺少原密钥时自动解密。升级不会删除旧缓存：

1. 已同步的数据在登录后正常载入。
2. 若浏览器仍保留有效的旧解密会话，首次进入新版会尝试自动导入。
3. 若有未同步的旧笔记或缓存，在“设置与数据 → 可选：导入旧版未同步的本地数据”中输入原口令完成一次导入。导入完成后不再需要该口令，原副本保留。

取消独立口令后，本地缓存的保护依赖当前浏览器与设备，不再提供独立密码保护。退出账号会清除当前会话中的账号选择和解密状态，缓存文件仍分账号保留。

## 云同步的范围

从“设置与数据”进入云服务页面，也可访问 `/cloud`。由设置入口进入时，会先尝试将本地阅读记录同步到站点。

| 云服务 | 接入方式 |
| --- | --- |
| OneDrive | 官方 OAuth，应用专用目录 |
| Google Drive | 官方 OAuth，应用专用隐藏目录 |
| 百度网盘 | 通过已挂载百度网盘的 WebDAV 服务接入 |
| 阿里云盘 | 通过已挂载阿里云盘的 WebDAV 服务接入 |
| WebDAV / Nextcloud / ownCloud | HTTPS WebDAV 地址、用户名和应用密码 |

百度网盘、阿里云盘在此版本中没有原生 OAuth/API 适配器，需要自行提供具备读写权限的 WebDAV 挂载服务；不要将网盘登录密码填入 WebDAV 密码框。

“上传备份”将当前站点账号的书籍文件、书目、进度、书签、笔记、设置保存为应用格式备份。“合并恢复”导入缺少的书籍，并合并较新的阅读记录；已有同内容书籍不重复入库，不删除本地或远端书籍。已存在书籍的书目字段保留现状。

备份使用 `cr-用户ID-` 前缀，应用不会混用不同用户的备份。当前版本用于同一站点、同一账号恢复，不提供跨站点账号映射，也不是网盘任意目录的文件浏览器。

文件按 8 MB 分片传输，任务进度存于 D1。网络失败或关闭页面后可在云服务页面继续未完成任务。最后写入备份索引，未完成的备份不替换上一份有效索引。

自动备份需要勾选开关，仅在云服务页面打开且联网时，每 15 分钟运行；浏览器关闭时不会继续执行。它不是云端常驻双向实时同步。

单次备份支持最多 1000 本书、5000 条阅读记录，索引最大 1 MB，单本最大 200 MB；超出限制会明确报错，不会静默截断。任务有效期 24 小时。恢复遵守个人和站点容量限制及 5% 预留空间。取消/过期任务会清理未发布的恢复文件；远端旧分片不会自动删除。

## OneDrive 配置

在 Microsoft Entra 应用注册中创建 Web 应用，选择适合用户的账号类型。添加 Microsoft Graph 委托权限 `Files.ReadWrite.AppFolder`，并允许离线授权。创建客户端密钥。

在 Cloudflare 项目中配置：

| 名称 | 内容 |
| --- | --- |
| `ONEDRIVE_CLIENT_ID` | 应用（客户端）ID |
| `ONEDRIVE_CLIENT_SECRET` | 客户端密钥，建议作为 Secret |

注册的 Web 回调地址必须与实际站点完全一致：`https://你的域名/cloud/oauth/onedrive/callback`。

部署后，在云服务页面点击“连接 OneDrive”，使用自己的 Microsoft 账号授权。应用会在专用目录中存放分片和索引。

参考：[Microsoft 应用专用文件夹](https://learn.microsoft.com/en-us/graph/onedrive-sharepoint-appfolder)、[OAuth 授权码流程](https://learn.microsoft.com/en-us/entra/identity-platform/v2-oauth2-auth-code-flow)。

## Google Drive 配置

在 Google Cloud 项目中启用 Google Drive API，配置 OAuth 同意屏幕，并创建“Web 应用”类型的 OAuth 客户端。授权范围使用 `https://www.googleapis.com/auth/drive.appdata`。测试状态下将使用者加入测试用户列表；是否需要发布或审核，以控制台要求为准。

在 Cloudflare 项目中配置：

| 名称 | 内容 |
| --- | --- |
| `GOOGLE_CLIENT_ID` | OAuth 客户端 ID |
| `GOOGLE_CLIENT_SECRET` | OAuth 客户端密钥，建议作为 Secret |

注册回调地址：`https://你的域名/cloud/oauth/google/callback`。

部署后点击“连接 Google Drive”并授权。备份位于应用专用隐藏空间，不会以普通书籍文件出现在网盘主目录。

参考：[Google 服务端 OAuth](https://developers.google.com/identity/protocols/oauth2/web-server)、[Drive 上传流程](https://developers.google.com/workspace/drive/api/guides/manage-uploads)。

## WebDAV、百度网盘、阿里云盘等

1. 准备一个支持 HTTPS 及文件读取、写入、PROPFIND 的 WebDAV 服务。百度/阿里需先在该服务中挂载相应网盘，并开放可写目录。
2. 预先创建用于备份的目录，例如 `https://dav.example.com/CloudReader/`。
3. 在 Cloudflare 项目配置 `WEBDAV_ALLOWED_HOSTS=dav.example.com`。多个域名使用英文逗号分隔，只填域名，不填协议或路径。不支持通配符；当前支持标准 HTTPS 443 端口。
4. 在对应服务卡片中填写完整目录 URL、WebDAV 用户名和应用密码，点击“验证并保存”。
5. 点击“测试连接”，再执行备份或恢复。

应用禁止自动跟随 WebDAV 重定向，以免将凭据转发到其他服务；请填写最终 WebDAV 地址。浏览器不直接访问 WebDAV，因此不需要额外浏览器 CORS 配置。Cloudflare Worker 必须能够访问该地址。

## 已完成验证与限制

- 实际 Worker + SQLite：多用户鉴权、书籍/上传/同步隔离、配额、加密连接凭据、WebDAV 域名限制、任务隔离、失败后继续、两分片备份恢复、文件逐字节一致性、重复恢复去重、OAuth 状态校验和防重放。
- 模拟远端服务：Google Drive、OneDrive、WebDAV 的授权/传输协议适配器。百度与阿里复用 WebDAV 适配器，未连接实际挂载服务。
- 本机 Edge：1440×960 和 390×844 页面；无独立口令登录、下载设置、字体/颜色持久化、恢复默认、账号缓存隔离、旧版批注迁移、断网打开已缓存整本书、云服务配置与测试连接按钮。
- 尚未部署到真实 Cloudflare，未使用真实 Microsoft/Google/百度/阿里账号或真实 WebDAV 服务联调。需要完成上面的应用与服务配置后进行线上验证。

源代码构建入口为工作区的 `reader-next/build.mjs`，它在保留 v1.1.0 的基础上生成本部署包。原 v1.0.0/v1.1.0 文件保持不变。
