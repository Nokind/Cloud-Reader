# v1.2.2 管理员配置

在现有 Cloudflare Pages 项目 → Settings → Variables and Secrets → 生产环境，配置：

- ADMIN_USERNAME：管理员用户名（3–32 位英文字母、数字、下划线或连字符）。
- ADMIN_PASSWORD：管理员密码（8–256 位），类型选 Secret。

保留现有 DB、BOOKS、DEPLOY_KEY、PASSWORD_PEPPER 等配置。上传本目录中的部署文件，重新部署后直接使用上述账号密码登录。

已有站点配置这两项后会更新原始管理员账号，保留其用户 ID、书库和记录。新密码应用后原管理员登录会话失效。未设置这两项的旧站点保持原登录方式；新站点必须设置。用户名不能与其他用户冲突。

管理员密码通过 Pages 修改并重新部署，无需清空数据库。网页首次创建管理员接口已关闭。版本接口应返回 cloud-reader-v1.2.2。

