# Cloud Reader v1.4.0

新增功能：

- 首页继续阅读，展示最近四本书。
- TXT 智能章节识别、EPUB 原生目录和 PDF 文档大纲，支持搜索跳转。
- 连续滚动，章节或页面自动接续，同时渲染最多五节/页。
- 离线下载队列，支持暂停、刷新后续传、批量加入、校验和缓存移除。
- 今日/累计阅读时长、阅读天数、已读数及最近七天统计。
- 书籍信息与封面编辑，支持上传缩略图和提取内置封面。
- 青绿、暖纸、深色、极简四种网站风格。

部署：

将本包上传现有 Cloudflare Pages 项目。部署根目录直接包含 _worker.js、_routes.json、index.html。保留 DB、BOOKS、DEPLOY_KEY、PASSWORD_PEPPER、ADMIN_USERNAME、ADMIN_PASSWORD 及云服务配置，无需新建数据库或添加环境变量。

部署后刷新，/api/v1/version 应返回 cloud-reader-v1.4.0。无需清空浏览器数据。旧阅读设置保持原样；需要连续阅读时，在阅读设置的翻页模式中选择“连续滚动 · 自动接续章节”。

使用入口：

首页“继续阅读”区域可切换网站风格、打开下载管理与统计。书籍“管理”按钮可修改资料及封面。阅读器“目录”支持章节搜索。

下载只在页面打开期间执行，刷新后需手动继续。目录按需生成。统计从本版本开始计算。详细说明和格式限制见 README.md。

已通过本地桌面/移动端和接口验证；尚未代为部署线上站点。
